import React from 'react';

const Settings = () => {
  return (
    <div>
      <h2>Settings</h2>
      <p>Configure your account settings here.</p>
      {/* Add settings options such as password change, notification preferences, etc. */}
    </div>
  );
};

export default Settings;
